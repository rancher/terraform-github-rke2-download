name: FOSSA Scanning

on:
  push:
    branches: ["main", "master", "release/**"]
  workflow_dispatch:

<<<<<<< Updated upstream
permissions:
  contents: read
  id-token: write
=======
permissions: {}
>>>>>>> Stashed changes

jobs:
  fossa-scanning:
    runs-on: ubuntu-latest
    timeout-minutes: 30
<<<<<<< Updated upstream
    steps:
    - name: Checkout
      uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6

    # The FOSSA token is shared between all repos in Rancher's GH org. It can be
    # used directly and there is no need to request specific access to EIO.
    - name: Read FOSSA token
      uses: rancher-eio/read-vault-secrets@7282bf97898cd1c16c89f837e0bb442e6d384c89 # latest https://github.com/rancher-eio/read-vault-secrets/commits/main/
      with:
        secrets: |
          secret/data/github/org/rancher/fossa/push token | FOSSA_API_KEY_PUSH_ONLY

    - name: FOSSA scan
      uses: fossas/fossa-action@ff70fe9fe17cbd2040648f1c45e8ec4e4884dcf3 # v1.9.0 https://github.com/fossas/fossa-action/releases
      with:
        api-key: ${{ env.FOSSA_API_KEY_PUSH_ONLY }}
        # Only runs the scan and do not provide/returns any results back to the
        # pipeline.
        run-tests: false
=======
    container:
      image: ghcr.io/rancher/ci-image/nix:20260603-18
    permissions:
      contents: read
      id-token: write
    steps:
      - name: 'Checkout'
        # https://github.com/actions/checkout/releases
        uses: actions/checkout@9f698171ed81b15d1823a05fc7211befd50c8ae0 # v6.0.3

      # The FOSSA token is shared between all repos in Rancher's GH org.
      # It can be used directly and there is no need to request specific access to EIO.
      - name: 'Read FOSSA Token'
        # https://github.com/rancher-eio/read-vault-secrets/commits/main/
        uses: rancher-eio/read-vault-secrets@7282bf97898cd1c16c89f837e0bb442e6d384c89 # main
        with:
          secrets: |
            secret/data/github/org/rancher/fossa/push token | FOSSA_API_KEY_PUSH_ONLY

      - name: 'FOSSA Scan'
        # https://github.com/fossas/fossa-action/releases
        uses: fossas/fossa-action@ff70fe9fe17cbd2040648f1c45e8ec4e4884dcf3 # v1.9.0
        with:
          api-key: ${{ env.FOSSA_API_KEY_PUSH_ONLY }}
          # Only runs the scan and do not provide/returns any results back to the pipeline.
          run-tests: false
>>>>>>> Stashed changes
